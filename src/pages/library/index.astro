---
// src/pages/library/index.astro
import Garden from '../../layouts/Garden.astro';
import { getCollection } from 'astro:content';

const entries = await getCollection('library');
const sorted = entries.sort((a, b) => b.data.date_added.getTime() - a.data.date_added.getTime());
---
<Garden title="library">
  <div class="page-wrapper">
    <div class="page-header">
      <div class="page-title-row">
        <h1 class="section-header">library</h1>
        <span class="page-phonetic">[lahy-brer-ee] &nbsp;/ ˈlaɪ.brɛr.i /</span>
      </div>
      <p class="page-def">books, films, albums, and shows worth keeping.</p>
    </div>
    <div class="book-grid">
      {sorted.map(entry => {
        const d = entry.data;
        return (
          <div class="book-card">
            <div class="book-cover-wrap">
              {d.cover_image
                ? <img class="book-cover" src={d.cover_image} alt={d.title} loading="lazy" />
                : <div class="book-cover book-cover--placeholder" />
              }
              {d.status && (
                <span class:list={['book-status', `book-status--${d.status.replace(' ', '-')}`]}>
                  {d.status}
                </span>
              )}
              {d.link && (
                <a href={d.link} target="_blank" rel="noopener noreferrer" class="cover-overlay" aria-label={`View ${d.title}`}>
                  <span class="cover-pill">view ↗</span>
                </a>
              )}
            </div>
            <p class="book-title">{d.title}</p>
            <p class="book-author">{d.creator}</p>
          </div>
        );
      })}
    </div>
  </div>
</Garden>

<style>

  .book-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: var(--space-xxl) var(--space-xl);
    padding-bottom: var(--space-section);
  }

  @media (max-width: 600px) {
    .book-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .book-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .book-cover-wrap {
    position: relative;
    display: block;
    border-radius: 3px 6px 6px 3px;
    overflow: hidden;
  }

  /* spine + edge shadow overlay */
  .book-cover-wrap::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 3px 6px 6px 3px;
    pointer-events: none;
    background:
      linear-gradient(to right, rgba(0,0,0,0.15) 0%, rgba(0,0,0,0.1) 0.5%, rgba(0,0,0,0) 3%, rgba(0,0,0,0.1) 5.5%, rgba(0,0,0,0.15) 6%, rgba(0,0,0,0.15) 6.5%, rgba(0,0,0,0.1) 7.5%, rgba(255,255,255,0.06) 20%, rgba(0,0,0,0) 80%, rgba(0,0,0,0.1) 100%),
      linear-gradient(to bottom, rgba(0,0,0,0.15) 0%, transparent 5%),
      linear-gradient(to top, rgba(0,0,0,0.15) 0%, transparent 1%);
  }

  .book-cover {
    width: 100%;
    aspect-ratio: 2 / 3;
    object-fit: cover;
    display: block;
    border-radius: 3px 6px 6px 3px;
    transition: filter 0.2s ease;
  }

  .book-cover--placeholder {
    background: var(--color-surface);
    aspect-ratio: 2 / 3;
  }

  /* status badge — upper right corner */
  .book-status {
    position: absolute;
    top: 8px;
    right: 8px;
    padding: 3px 8px;
    border-radius: 999px;
    font-family: var(--font-body);
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    line-height: 1;
    pointer-events: none;
    backdrop-filter: blur(6px);
  }

  .book-status--read {
    background: rgba(155, 190, 155, 0.88);
    color: #1e3a1e;
  }
  .book-status--in-progress {
    background: rgba(240, 210, 140, 0.92);
    color: #4a3300;
  }
  .book-status--to-read {
    background: rgba(255, 255, 255, 0.75);
    color: #5a4a38;
  }

  /* darkening overlay + pill — only rendered when link exists */
  .cover-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 0, 0, 0);
    text-decoration: none;
    border-radius: 12px;
    transition: background 0.2s ease;
  }

  .book-cover-wrap:hover .book-cover {
    filter: brightness(0.65);
  }

  .book-cover-wrap:hover .cover-overlay {
    background: rgba(0, 0, 0, 0.15);
  }

  .cover-pill {
    background: #ffffff;
    color: #3d2f1f;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 600;
    letter-spacing: 0.04em;
    padding: 6px 16px;
    border-radius: 999px;
    opacity: 0;
    transform: translateY(6px);
    transition: opacity 0.2s ease, transform 0.2s ease;
    pointer-events: none;
  }

  .book-cover-wrap:hover .cover-pill {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .book-title {
    font-family: var(--font-serif);
    font-size: var(--text-md);
    font-weight: 700;
    color: var(--color-fg);
    margin: 0;
    line-height: var(--leading-tight);
  }

  .book-author {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-muted);
    margin: 0;
  }
</style>
